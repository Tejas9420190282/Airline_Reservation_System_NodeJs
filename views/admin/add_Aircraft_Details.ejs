<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Jet Registration</title>
        <link
            rel="stylesheet"
            href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
        />
    </head>
    
    <%- include("../navbar") %>

    <body class="flex justify-center items-center min-h-screen bg-gray-100">
        <div class="bg-white p-6 rounded-lg shadow-md w-96">
            <h2 class="text-2xl font-bold mb-4 text-center">Register a Jet</h2>

            <form action="/submit-jet-details" method="POST" class="space-y-4">
                <div>
                    <label class="block text-sm font-medium text-gray-700"
                        >Enter Valid Jet ID</label
                    >
                    <input
                        type="text"
                        name="jetId"
                        required
                        class="w-full p-2 border border-gray-300 rounded mt-1"
                        placeholder="Enter Jet ID"
                    />
                </div>

                <div>
                    <label class="block text-sm font-medium text-gray-700"
                        >Jet Type</label
                    >
                    <input
                        type="text"
                        name="jetType"
                        value="Commercial Flight"
                        readonly
                        class="w-full p-2 border border-gray-300 rounded mt-1 bg-gray-200"
                    />
                </div>

                <div>
                    <label class="block text-sm font-medium text-gray-700"
                        >Enter Total Capacity</label
                    >
                    <input
                        type="number"
                        name="capacity"
                        required
                        class="w-full p-2 border border-gray-300 rounded mt-1"
                        placeholder="Enter Capacity"
                    />
                </div>

                <button
                    type="submit"
                    class="w-full bg-blue-500 text-white py-2 rounded hover:bg-blue-600"
                >
                    Submit
                </button>
            </form>
        </div>
    </body>
</html>
