<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Dashboard</title>
        <!-- Tailwind CSS -->
        <script src="https://cdn.tailwindcss.com"></script>
    </head>

    <main class="container mx-auto mt-6 p-6 bg-white rounded shadow-md">

        <!-- Displaying the passed message -->
        <div class="mt-4">
            <% if (message) { %>
            <div
                class="p-4 bg-green-100 text-green-800 rounded border border-green-300"
            >
                <%= message %>
            </div>
            <% } %>
        </div>
    </main>
</html>
